<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amazon Inventory Tracker</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <meta http-equiv="refresh" content="30">
    <style>
        /* This keeps the long titles readable but compact */
        .title-clamp {
            display: -webkit-box;
            -webkit-line-clamp: 2; /* Shows exactly 2 lines of text */
            -webkit-box-orient: vertical;
            overflow: hidden;
            font-size: 0.75rem; /* Smaller text size (text-xs) */
            line-height: 1rem;
        }
    </style>
</head>
<body class="bg-slate-50 p-4 md:p-8">

    <div class="max-w-full mx-auto">
        <header class="flex justify-between items-center mb-6">
            <div>
                <h1 class="text-2xl font-bold text-slate-900">Inventory Dashboard</h1>
                <p class="text-slate-500 text-sm">Real-time status of your Amazon listings.</p>
            </div>
            <div class="text-right">
                <p class="text-xs font-semibold text-slate-400 uppercase">Live Progress</p>
                <p class="text-sm font-medium text-slate-700"><%= new Date().toLocaleTimeString() %></p>
            </div>
        </header>

        <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
            <table class="w-full text-left border-collapse table-auto">
                <thead>
                    <tr class="bg-slate-900 text-white text-xs uppercase tracking-wider">
                        <th class="p-3">Status</th>
                        <th class="p-3">ASIN</th>
                        <th class="p-3 w-1/3">Full Product Title</th>
                        <th class="p-3 text-center">Stock</th>
                        <th class="p-3">Seller</th>
                        <th class="p-3">Price</th>
                        <th class="p-3 text-center">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    <% reports.forEach(item => { %>
                        <tr class="<%= item.is_changed ? 'bg-red-50' : 'hover:bg-slate-50' %> transition-colors">

                            <td class="p-3 whitespace-nowrap">
                                <% if (item.is_doggy) { %>
                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-orange-100 text-orange-800">🐶 DOGGY</span>
                                <% } else { %>
                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-blue-100 text-blue-800">✅ ACTIVE</span>
                                <% } %>
                            </td>

                            <td class="p-3">
                                <a href="https://www.amazon.ca/dp/<%= item.asin %>" target="_blank" class="font-mono text-xs text-blue-600 hover:underline font-bold">
                                    <%= item.asin %>
                                </a>
                            </td>

                            <td class="p-3">
                                <div class="title-clamp text-slate-600 font-medium" title="<%= item.header %>">
                                    <%= item.header %>
                                </div>
                            </td>

                            <td class="p-3 text-center whitespace-nowrap">
                                <span class="px-2 py-0.5 text-[10px] font-black rounded <%= item.availability === 'In Stock' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %> uppercase">
                                    <%= item.availability %>
                                </span>
                            </td>

                            <td class="p-3 text-xs text-slate-500 italic truncate max-w-[100px]">
                                <%= item.seller %>
                            </td>

                            <td class="p-3 text-sm font-bold <%= item.is_changed ? 'text-red-600 animate-pulse' : 'text-slate-900' %>">
                                <%= item.price %>
                            </td>

                            <td class="p-3 text-center">
                                <a href="/history/<%= item.asin %>" class="px-2 py-1 border border-slate-300 text-[10px] font-bold rounded bg-white hover:bg-slate-100 transition">
                                    HISTORY
                                </a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>

</body>
</html>
